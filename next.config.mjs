// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   images: {
//     domains: ["storage.googleapis.com"],
//   },
// };

// export default nextConfig;
const nextConfig = {
  images: {
    domains: ["storage.googleapis.com/file-storage-d7ecd.appspot.com"],
  },
};

export default nextConfig;
